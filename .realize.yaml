settings:
  legacy:
    force: false
    interval: 0s
schema:
  - name: go-echo-practice
    path: .
    commands:
      run:
        status: true
    watcher:
      extensions:
        - go
      paths:
        - /
      ignored_paths:
        - .git
        - .realize
        - vendor
